---
import Layout from "@/layouts/page-layout.astro";
import { getBlogsContent } from "@/i18n/utils";

const blogs = getBlogsContent(Astro.currentLocale);

return Astro.redirect(blogs.announcement.link);
---

<Layout title={blogs.title} description={blogs.title}>
  <section class="relative pb-20"></section>
</Layout>
